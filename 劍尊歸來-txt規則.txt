1. "第(數字)章"這段文字的下面第三行就是每章的標題
2. 如果有一列有出現一串不明的數字和英文，則刪掉略過
3. 如果有一列有出現"漢 化"或"閒魚"，則刪掉略過此列
4. 文中所有的"清明"接直接改成"青明"
5. 有出現"@雨"字樣，直接刪掉略過

更新到git hub:
git add .
git commit -m "更新內容"
git pull origin main --rebase
git push origin main
